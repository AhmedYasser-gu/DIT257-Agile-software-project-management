"use client";
export default function CategoryPill({ label }: { label: string }) {
  return (
    <span className="text-xs px-2 py-0.5 rounded-full border border-border bg-white">
      {label}test
    </span>
  );
}

